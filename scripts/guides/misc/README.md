The `guides/misc` folder contains guides which do not fit into the other `guides` folders.

These scripts are intended to help users understand how **PyAutoLens** works.

# Files

- `witt_wynne`: Projecting a lens model onto Witt-Wynne (SIEP) space and the ported `isit4or2or1` quartic solver.
